import { ThemeSwitcher } from '@/components/other/ThemeSwitcher'
import { act, fireEvent, render, screen } from '@testing-library/react'

describe('Header', () => {
  it('change color scheme theme correctly', async () => {
    // render(<ThemeSwitcher />)
    // const button = screen.getByRole('button', { name: 'Tema Sistema' })
    // await act(() => {
    //   fireEvent.click(button)
    // })
    // const lightTheme = screen.getByRole('option', { name: 'Claro' })
    // await act(() => {
    //   fireEvent.click(lightTheme)
    // })
    // expect(button).toHaveAccessibleName('Tema Claro')
  })
})
